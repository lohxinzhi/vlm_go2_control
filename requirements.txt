# ROS 2 Jazzy's cv_bridge binary is built against NumPy 1.x.
numpy<2
opencv-python
# Keep this constraint in sync with vlm_go2_control/setup.py.
openai>=1.0.0,<3.0.0
pyyaml
colcon-common-extensions
# ROS interface generation and the package's colcon tests.
lark==1.3.1
pytest==8.4.2
flake8==7.4.1
pydocstyle==6.3.0
